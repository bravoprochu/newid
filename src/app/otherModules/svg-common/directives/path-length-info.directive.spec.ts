import { PathLengthInfoDirective } from './path-length-info.directive';

describe('PathLengthInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new PathLengthInfoDirective();
    expect(directive).toBeTruthy();
  });
});
